import React from "react";
import ConnectedCounter from "./components/Counter"; // Import the connected Counter component

// App component
const App = () => <ConnectedCounter />;

export default App;
