const User = require("../models/userModel");
const argon2 = require("argon2");

// #7. Make a function that will register a new user. Use res.json() while testing the backend
async function registerNewUser(req, res) {
  // #14: When testing for front-end user registration, use res.redirect()
}

// #19: Use this function to log in. Use res.redirect('/')
async function login(req, res) {}

module.exports = {
  // registerNewUser,
  // login
};
