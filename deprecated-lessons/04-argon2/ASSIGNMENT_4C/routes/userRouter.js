const router = require("express").Router();

const {
  // registerNewUser,
  // login
} = require("../controllers/userController");

// #8: Create a route at this URL extension
// localhost:8080/user/registerNewUser

// #20: Create a route at this URL extension
// localhost:8080/user/login

module.exports = router;
